import USlider from '@/comps/USlider'
import { defineComponent } from 'vue'

const SliderDemoPage = defineComponent({
  name: 'SliderDemoPage',
  setup() {
    return () => <USlider />
  }
})

export default SliderDemoPage
